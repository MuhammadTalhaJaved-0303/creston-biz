import { StackScene } from "@/components/sections/stack/StackScene";

/** How we work: the engagement pulled apart into its five layers, on a cool blue ground. */
export function Stack() {
  return (
    <section id="stack" className="bg-ground-stack relative border-b border-line">
      <StackScene />
    </section>
  );
}
