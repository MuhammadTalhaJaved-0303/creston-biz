import { StackScene } from "@/components/sections/stack/StackScene";

/** How we work: the engagement pulled apart into its five layers. */
export function Stack() {
  return (
    <section id="stack" className="relative border-y border-line bg-surface bg-mesh-soft">
      <StackScene />
    </section>
  );
}
